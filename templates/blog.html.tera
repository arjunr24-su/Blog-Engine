<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
    <header>
        <h1>{{ title }}</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/category/tech">Tech</a></li>
                <li><a href="/category/security">Security</a></li>
                <li><a href="/posts">All Posts</a></li> <!-- Link to view all posts -->
            </ul>
        </nav>
    </header>

    <main>
        <!-- Display Dev.to blog posts -->
        <h2>Dev.to Blog Posts</h2>
        <ul>
            {% for article in devto_articles %}
                <li>
                    <h3><a href="{{ article.url }}">{{ article.title }}</a></h3>
                    <p>{{ article.description | raw }}</p> <!-- Render HTML content -->
                </li>
            {% else %}
                <li>No Dev.to articles found.</li>
            {% endfor %}
        </ul>

        <!-- Display Hacker News stories -->
        <h2>Hacker News Stories</h2>
        <ul>
            {% for story in hacker_news_articles %}
                <li>
                    <h3><a href="{{ story.url }}">{{ story.title }}</a></h3>
                </li>
            {% else %}
                <li>No Hacker News stories found.</li>
            {% endfor %}
        </ul>

        <!-- Display All Blog Posts -->
        <h2>All Blog Posts</h2>
        <ul>
            {% for article in articles %}
                <li>
                    <h3><a href="{{ article.url }}">{{ article.title }}</a></h3>
                    <p>{{ article.description | raw }}</p> <!-- Display parsed HTML -->
                    <span>Category: {{ article.category }}</span>
                </li>
            {% else %}
                <li>No articles found.</li>
            {% endfor %}
        </ul>
    </main>

    <footer>
        <p>&copy; 2024 Blog Engine</p>
    </footer>
</body>
</html>
